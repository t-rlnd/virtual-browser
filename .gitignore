node_modules/

# Configuration locale de l'outillage : declare comment lancer le serveur
# de developpement, ce que package.json dit deja. Propre a la machine.
.claude/

# Sortie du mode watch. La sortie de production, elle, vit dans dist/
# et est versionnee : jsDelivr la lit depuis le depot.
dev/
masters/
public/assets/
exports/
.playwright/
.artifacts/
.DS_Store
.env
.env.local

# dist/ est volontairement versionne : jsDelivr sert le bundle depuis
# GitHub. Les medias, eux, restent hors depot (Bunny les porte).

# Masters egares a la racine
/*.mp4
