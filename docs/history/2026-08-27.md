# 2026-08-27

- Mode compact sous 991 px : plus de scrub, sections empilees, video en
  boucle dans son cadre, pause hors ecran (`playback.js`, `compact.js`).
- Tag `v1.3.0` : bundle servi via jsDelivr.
- Revue de la documentation interne : stack et points d'entree ajoutes a
  `architecture.md`, ADR `0003` (decoupage sur la balise).
